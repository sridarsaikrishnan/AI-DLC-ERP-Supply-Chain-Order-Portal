# Tech Stack Decisions — U4 Admin & Configuration

Inherits U0. Minimal admin = JSON API (no served HTML for MVP). Uses FastAPI router, U0 config tables/models, U1 ADMIN guard, U3 AdapterRegistry for connectivity checks. No new libraries.
